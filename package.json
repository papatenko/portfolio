{
  "name": "papatenko-portfolio",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "build:dev": "turbo run build --filter=dev",
    "build:creative": "turbo run build --filter=creative"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
