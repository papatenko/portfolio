name: Deploy to Portfolio Hosting

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

jobs:
  build:
    name: Build Docker Image and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: CI
        uses: actions/checkout@v4 # officially supported action
      - run: npm ci # like npm install but for CI server
      - run: npm run build

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: papatenko/portfolio:latest

      # - name: Deploy
      #   uses: appleboy/ssh-action@master
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USERNAME }}
      #     password: ${{ secrets.SSH_PASSWORD }}
      #     key: ${{ secrets.SSH_KEY }}
      #     script: |
      #       docker image pull papatenko/portfolio:latest
      #       docker container stop portfolio
      #       docker container rm portfolio
      #       docker container run -d -p 4000:4000 --name portfolio papatenko/portfolio:latest
